package com.example.aptiv.Model.Interface;


//interface to have unified function between all fragments that needs information when a zone is selected in parent fragment
public interface IZoneSelection {
    void zoneIsSelected();
}
